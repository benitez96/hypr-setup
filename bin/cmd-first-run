#!/bin/bash

set -e

FIRST_RUN_MODE=~/.local/state/hypr/first-run.mode

if [[ -f "$FIRST_RUN_MODE" ]]; then
  rm -f "$FIRST_RUN_MODE"

  bash "$HYPR_SETUP_PATH/install/first-run/battery-monitor.sh"
  bash "$HYPR_SETUP_PATH/install/first-run/cleanup-reboot-sudoers.sh"
  bash "$HYPR_SETUP_PATH/install/first-run/firewall.sh"
  bash "$HYPR_SETUP_PATH/install/first-run/dns-resolver.sh"
  bash "$HYPR_SETUP_PATH/install/first-run/gnome-theme.sh"
  sudo rm -f /etc/sudoers.d/first-run

  bash "$HYPR_SETUP_PATH/install/first-run/welcome.sh"
  bash "$HYPR_SETUP_PATH/install/first-run/wifi.sh"
fi
