#!/bin/bash

# Close all open windows
hyprctl clients -j | \
  jq -r ".[].address" | \
  xargs -I{} hypr-dispatch "hl.dsp.window.close({ window = 'address:{}' })" "closewindow address:{}"

# Move to first workspace
hypr-dispatch "hl.dsp.focus({ workspace = 1 })" "workspace 1"
