#!/usr/bin/env bash
# Returns the battery percentage remaining as an integer.

upower -i $(upower -e | grep BAT) 2>/dev/null | \
awk -F: '/percentage/ {
    gsub(/[%[:space:]]/, "", $2);
    val=$2;
    printf("%d\n", (val+0.5))
    exit
  }' || echo "0"

