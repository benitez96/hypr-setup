#!/usr/bin/env bash

DIR="$HOME/Pictures/Screenshots"
mkdir -p "$DIR"

case "$1" in
  area)
    grim -g "$(slurp)" - | swappy -f - &
    ;;
  full)
    grim "$DIR/$(date +'%Y-%m-%d_%H-%M-%S').png"
    ;;
  *)
    echo "Uso: screenshot {area|full}"
    ;;
esac
