#!/usr/bin/env bash

choice="$(printf '%s\n' \
  'WiFi' \
  'Bluetooth' \
  | walker --dmenu --prompt 'Network: ')"

case "$choice" in
  'WiFi')
    launch-wifi
    ;;
  'Bluetooth')
    launch-bluetooth
    ;;
  *)
    exit 0
    ;;
esac

